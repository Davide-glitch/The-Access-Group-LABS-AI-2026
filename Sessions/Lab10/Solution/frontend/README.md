# Lab 10 — frontend (given, not built this session)

This is Lab 9's finished Solution, unchanged — included here so the Lab 10
reference solution is fully self-contained. Lab 10's actual new work is in
[`../backend`](../backend): `POST /quizzes/generate` as an AI agent,
`GET /quizzes/{id}/play`, and `POST /quizzes/{id}/evaluate`.

```bash
npm install
npm run dev
# → http://localhost:5173
```

The API in [`../backend`](../backend) must be running at
`http://localhost:5023`. Sign in with Microsoft before generating or
creating a quiz — both are writes.
