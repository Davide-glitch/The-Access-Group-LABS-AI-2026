# Lab 5 — Code

This folder is intentionally empty. Your `QuizzesApi` project gets created here in **Step 1** of `../guide.html`.

Start the guide and run the `dotnet new` command from inside this folder.
